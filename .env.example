# Example environment variables for local development
# Do NOT store real secrets here or commit them with real values.
# Copy this file to .env.local and fill in your real values for local testing.

# Gemini API key used by the proxy server (set this in Vercel for production)
GEMINI_API_KEY=
